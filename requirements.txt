# CV-Toposheet dependencies
# Install with: pip install -r requirements.txt

# Web interface
flask>=3.0.0

# Environment / config
python-dotenv>=1.0.0

# Image processing + tiling
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.24.0

# OCR (primary)
easyocr>=1.7.0

# LLM APIs
openai>=1.30.0
google-genai>=1.0.0

# Database
# sqlite3 is built-in to Python — no install needed

# Data handling + export
pandas>=2.0.0

# Progress bars
tqdm>=4.66.0

# CPU monitoring + thermal throttle (cross-platform)
psutil>=5.9.0
